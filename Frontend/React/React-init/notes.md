What is bundler?
 - bundler bundles your code so that it can be shift to production. Compress or optimise code.
 Examples of boundler : parcel, vite


will a bundler be dev dependency or normal dependency?
 - dev dependency because we don't need for production.It also needs for development.


will a React be dev dependency or normal dependency?
 -  React is normal dependency because it needs development and production both.Compressed React also stores in dist folder.